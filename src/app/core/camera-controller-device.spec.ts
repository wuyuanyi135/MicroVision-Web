import { CameraControllerDevice } from './camera-controller-device';

describe('CameraControllerDevice', () => {
  it('should create an instance', () => {
    expect(new CameraControllerDevice()).toBeTruthy();
  });
});
